---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Navigation from '../../components/global/Navigation.astro';
import Footer from '../../components/global/Footer.astro';
import RoleGuard from '../../components/auth/RoleGuard.astro';
import QuotaDisplay from '../../components/auth/QuotaDisplay.astro';
// import PermissionGuard from '../../components/auth/PermissionGuard.astro'; // 暂时注释掉未使用的导入
// import AuthStatus from '../../components/auth/AuthStatus.astro'; // 暂时注释掉未使用的导入

// 用户会话将在客户端获取
const userData = null;

// 如果用户未登录，重定向到登录页面
if (!userData) {
  return Astro.redirect('/admin/login?redirect=/premium/dashboard');
}

const userId = userData.id;
const userEmail = userData.email;

// 获取用户角色信息
let userRole = null;
let roleError = null;

try {
  const response = await fetch(`${Astro.url.origin}/api/auth/get-user-role`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ userId })
  });

  if (response.ok) {
    const result = await response.json();
    userRole = result;
  } else {
    const error = await response.json();
    roleError = error.error;
  }
} catch (error) {
  roleError = 'Network error';
  console.error('Role fetch error:', error);
}
---

<BaseLayout title="付费用户面板 - BacklinksBuilder">
  <Navigation />
  
  <main class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- 页面标题 -->
      <div class="mb-8">
        <div class="flex items-center">
          <svg class="w-8 h-8 text-blue-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          <div>
            <h1 class="text-3xl font-bold text-gray-900">高级用户面板</h1>
            <p class="mt-2 text-gray-600">享受专属功能和优先支持</p>
          </div>
        </div>
      </div>

      <!-- 角色守卫：确保只有高级用户或管理员可以访问 -->
      <RoleGuard allowedRoles={['premium', 'admin']} userId={userId}>
        
        <!-- 高级用户欢迎横幅 -->
        <div class="bg-gradient-to-r from-blue-600 to-indigo-600 rounded-lg shadow-lg mb-6 text-white">
          <div class="px-6 py-8">
            <div class="flex items-center justify-between">
              <div>
                <h2 class="text-2xl font-bold mb-2">欢迎回来，高级用户！</h2>
                <p class="text-blue-100">您正在享受我们的高级服务，包括更高的配额和专属功能</p>
              </div>
              <div class="hidden md:block">
                <svg class="w-16 h-16 text-blue-200" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
              </div>
            </div>
          </div>
        </div>

        <!-- 用户信息卡片 -->
        <div class="bg-white shadow-lg rounded-lg mb-6">
          <div class="px-6 py-4 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-900">账户信息</h2>
          </div>
          <div class="px-6 py-4">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
              <div>
                <label class="block text-sm font-medium text-gray-700">邮箱地址</label>
                <p class="mt-1 text-sm text-gray-900">{userEmail}</p>
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700">用户角色</label>
                <p class="mt-1">
                  {userRole ? (
                    <span class={`inline-flex px-3 py-1 text-sm font-semibold rounded-full ${
                      userRole.role === 'admin' ? 'bg-purple-100 text-purple-800' :
                      'bg-blue-100 text-blue-800'
                    }`}>
                      {userRole.roleDetails?.display_name || userRole.role}
                    </span>
                  ) : (
                    <span class="text-sm text-gray-500">加载中...</span>
                  )}
                </p>
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700">会员状态</label>
                <p class="mt-1">
                  <span class="inline-flex px-3 py-1 text-sm font-semibold rounded-full bg-green-100 text-green-800">
                    高级会员
                  </span>
                </p>
              </div>
            </div>
            {roleError && (
              <div class="mt-4 p-3 bg-red-50 border border-red-200 rounded-md">
                <p class="text-sm text-red-600">角色信息加载失败: {roleError}</p>
              </div>
            )}
          </div>
        </div>

        <!-- 高级配额使用情况 -->
        <div class="bg-white shadow-lg rounded-lg mb-6">
          <div class="px-6 py-4 border-b border-gray-200">
            <div class="flex items-center justify-between">
              <div>
                <h2 class="text-lg font-medium text-gray-900">高级配额</h2>
                <p class="text-sm text-gray-600">享受更高的配额限制和优先处理</p>
              </div>
              <span class="inline-flex px-3 py-1 text-xs font-semibold rounded-full bg-blue-100 text-blue-800">
                高级版
              </span>
            </div>
          </div>
          <div class="px-6 py-4">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
              <QuotaDisplay quotaType="dr_checks" userId={userId} refreshInterval={30000} />
              <QuotaDisplay quotaType="backlink_generations" userId={userId} refreshInterval={30000} />
              <QuotaDisplay quotaType="traffic_checks" userId={userId} refreshInterval={30000} />
            </div>
          </div>
        </div>

        <!-- 高级功能 -->
        <div class="bg-white shadow-lg rounded-lg mb-6">
          <div class="px-6 py-4 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-900">高级功能</h2>
            <p class="text-sm text-gray-600">专为高级用户设计的强大工具</p>
          </div>
          <div class="px-6 py-4">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              
              <!-- 批量DR检查 -->
              <div class="border border-blue-200 rounded-lg p-6 hover:shadow-lg transition-all bg-gradient-to-br from-blue-50 to-blue-100">
                <div class="flex items-center mb-4">
                  <svg class="w-8 h-8 text-blue-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                  </svg>
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">批量DR检查</h3>
                    <span class="inline-flex px-2 py-1 text-xs font-medium rounded bg-blue-200 text-blue-800">高级版</span>
                  </div>
                </div>
                <p class="text-gray-600 mb-4">一次性检查多个域名的DR评分，提高工作效率</p>
                <a href="/dr-checker?mode=batch" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 transition-colors">
                  开始批量检查
                </a>
              </div>

              <!-- 高级外链分析 -->
              <div class="border border-green-200 rounded-lg p-6 hover:shadow-lg transition-all bg-gradient-to-br from-green-50 to-green-100">
                <div class="flex items-center mb-4">
                  <svg class="w-8 h-8 text-green-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path>
                  </svg>
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">高级外链分析</h3>
                    <span class="inline-flex px-2 py-1 text-xs font-medium rounded bg-green-200 text-green-800">高级版</span>
                  </div>
                </div>
                <p class="text-gray-600 mb-4">深度分析外链质量，获得详细的竞争对手分析</p>
                <a href="/backlink-generator?mode=advanced" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-green-600 hover:bg-green-700 transition-colors">
                  高级分析
                </a>
              </div>

              <!-- 流量趋势分析 -->
              <div class="border border-purple-200 rounded-lg p-6 hover:shadow-lg transition-all bg-gradient-to-br from-purple-50 to-purple-100">
                <div class="flex items-center mb-4">
                  <svg class="w-8 h-8 text-purple-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"></path>
                  </svg>
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">流量趋势分析</h3>
                    <span class="inline-flex px-2 py-1 text-xs font-medium rounded bg-purple-200 text-purple-800">高级版</span>
                  </div>
                </div>
                <p class="text-gray-600 mb-4">查看历史流量趋势和预测分析</p>
                <a href="/traffic-checker?mode=trends" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-purple-600 hover:bg-purple-700 transition-colors">
                  查看趋势
                </a>
              </div>

              <!-- API访问 -->
              <div class="border border-indigo-200 rounded-lg p-6 hover:shadow-lg transition-all bg-gradient-to-br from-indigo-50 to-indigo-100">
                <div class="flex items-center mb-4">
                  <svg class="w-8 h-8 text-indigo-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
                  </svg>
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">API访问</h3>
                    <span class="inline-flex px-2 py-1 text-xs font-medium rounded bg-indigo-200 text-indigo-800">高级版</span>
                  </div>
                </div>
                <p class="text-gray-600 mb-4">通过API集成我们的服务到您的应用中</p>
                <button class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 transition-colors">
                  获取API密钥
                </button>
              </div>

              <!-- 优先支持 -->
              <div class="border border-yellow-200 rounded-lg p-6 hover:shadow-lg transition-all bg-gradient-to-br from-yellow-50 to-yellow-100">
                <div class="flex items-center mb-4">
                  <svg class="w-8 h-8 text-yellow-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192L5.636 18.364M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z"></path>
                  </svg>
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">优先支持</h3>
                    <span class="inline-flex px-2 py-1 text-xs font-medium rounded bg-yellow-200 text-yellow-800">高级版</span>
                  </div>
                </div>
                <p class="text-gray-600 mb-4">享受24/7优先技术支持和专属客服</p>
                <button class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-yellow-600 hover:bg-yellow-700 transition-colors">
                  联系支持
                </button>
              </div>

              <!-- 数据导出 -->
              <div class="border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-all bg-gradient-to-br from-gray-50 to-gray-100">
                <div class="flex items-center mb-4">
                  <svg class="w-8 h-8 text-gray-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                  </svg>
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">数据导出</h3>
                    <span class="inline-flex px-2 py-1 text-xs font-medium rounded bg-gray-200 text-gray-800">高级版</span>
                  </div>
                </div>
                <p class="text-gray-600 mb-4">导出分析结果为CSV、Excel等格式</p>
                <button class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-gray-600 hover:bg-gray-700 transition-colors">
                  导出数据
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- 使用统计 -->
        <div class="bg-white shadow-lg rounded-lg">
          <div class="px-6 py-4 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-900">使用统计</h2>
            <p class="text-sm text-gray-600">您的高级功能使用情况</p>
          </div>
          <div class="px-6 py-4">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
              <div class="text-center">
                <div class="text-3xl font-bold text-blue-600">156</div>
                <div class="text-sm text-gray-600">本月DR检查</div>
              </div>
              <div class="text-center">
                <div class="text-3xl font-bold text-green-600">89</div>
                <div class="text-sm text-gray-600">外链生成</div>
              </div>
              <div class="text-center">
                <div class="text-3xl font-bold text-purple-600">234</div>
                <div class="text-sm text-gray-600">流量检查</div>
              </div>
              <div class="text-center">
                <div class="text-3xl font-bold text-indigo-600">12</div>
                <div class="text-sm text-gray-600">API调用</div>
              </div>
            </div>
          </div>
        </div>

      </RoleGuard>
    </div>
  </main>

  <Footer />
</BaseLayout>

<style>
  .transition-all {
    transition: all 0.3s ease-in-out;
  }
  
  .transition-colors {
    transition: background-color 0.2s ease-in-out, color 0.2s ease-in-out;
  }
  
  .hover\:shadow-lg:hover {
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  }
</style>